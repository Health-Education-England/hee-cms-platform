definitions:
  config:
    /hippo:configuration/hippo:userroles/hee.feedback.site.viewer:
      jcr:primaryType: hipposys:userrole
      hipposys:description: Allows user research participants to view delivery-tier
        authentication enabled Feedback channel pages
